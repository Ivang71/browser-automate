run job apply flow (default)
```bash
uv run main.py
```

run toknes generation
```
uv run main.py --mode tokens
```


or explicitly
```bash
uv run main.py apply
```

run alternative flow (e.g. gmail)
```bash
uv run main.py gmail
```


